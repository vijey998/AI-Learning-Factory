# 04. GPT from scratch

Status: executable CPU reference. Chapter: CH-24.

## Run

`python code/milestone_labs.py --lab 04`

## Hardware

CPU; optional GPU.

## Deliverable

Assemble embeddings, blocks, normalization and LM head.

## Acceptance conditions

Check shapes, parameter counts, tied weights and gradient flow; no pretrained wrapper.

## Required experiment record

Environment and revision; input data and licenses; shape/dtype; seeds; correctness checks; warmup and synchronization; elapsed time and memory; measured vs estimated quantities; failures; next experiment.
